name: Deploy to s3
on:
  push:
    branches:
      - test
jobs:
  build:
    name: Build files
    runs-on: ubuntu-18.04
    steps: 
      - name: Setup node
        uses: actions/setup-node@v1
        with: 
            node-version: '10.x'
      - uses: actions/checkout@main
      - name: Build program project files
        run: |
          npm install
          npm run build-test
      - name: Deploy to s3
        env: 
            AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
            AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        run:
          aws s3 cp dist s3://philagov-vue-apps/the-latest-archive/test/ --recursive